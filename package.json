{
  "name": "PHX",
  "version": "1.0.0",
  "description": "-",
  "scripts": {
    "phx-start": "php -S localhost:8000 -t public",
    "phx-live": "concurrently \"php -S localhost:8000 -t public\" \"npx gulp\"",
    "phx-install": "composer install && composer dump-autoload && npm install",
	"phx-watch": "gulp watch"
  },
  "author": "I-As Dev",
  "license": "MIT",
  "devDependencies": {
    "browser-sync": "^3.0.3",
    "gulp": "^5.0.0",
    "concurrently": "^9.1.2"
  }
}
